# مستورد ملفات النظام - System Importer (v2.0)

## 📝 البرومبت (Prompt)

انسخ هذا البرومبت واستخدمه لإنشاء واجهة استيراد ملفات النظام في أي مشروع Next.js:

---

```
أريدك أن تبني لي واجهة تسمح لي برفع ملفات النظام بصيغة ملف tar.

## المتطلبات:

### 1. واجهة الرفع (Frontend - src/app/page.tsx):
- واجهة عربية جميلة تدعم السحب والإفلات (Drag & Drop)
- عرض تقدم الرفع والاستخراج بشريط تقدم
- عرض قائمة الملفات المستخرجة بعد الاستخراج
- زر "تشغيل النظام" يظهر بعد الاستخراج الناجح
- إعادة تحميل الصفحة تلقائياً بعد النشر الناجح
- تصميم responsive يعمل على الجوال والكمبيوتر
- دعم الملفات الكبيرة (500+ MB)
- عرض رسائل الخطأ بالتفصيل

### 2. API Endpoint - استخراج الملفات (src/app/api/upload-system/route.ts):

#### POST /api/upload-system
- استقبال ملف tar من FormData
- حفظ الملف في مجلد مؤقت (tmpdir)
- استخراج الملفات باستخدام أمر tar
- إنشاء مجلد `extracted-system` في جذر المشروع
- إرجاع قائمة الملفات وعددها
- التعامل مع الملفات الكبيرة (1GB)
- timeout 5 دقائق لعملية الاستخراج

#### GET /api/upload-system
- إرجاع حالة الاستخراج الحالية

### 3. API Endpoint - النشر والتشغيل (src/app/api/upload-system/deploy/route.ts):

#### POST /api/upload-system/deploy
- نسخ الملفات من `extracted-system` إلى جذر المشروع
- استبعاد: node_modules, .next, .git, dev.log
- تثبيت الاعتمادات (bun install)
- تهيئة Prisma إن وجد (bun run db:generate)
- نسخ ملف قاعدة البيانات إن وجد
- إنشاء ملف .restart لإعادة تشغيل الخادم
- إرجاع حالة النشر

#### GET /api/upload-system/deploy
- إرجاع حالة النشر الحالية

### 4. API Endpoint - حالة الاستخراج (src/app/api/upload-system/status/route.ts):

#### GET /api/upload-system/status
- إرجاع حالة الاستخراج (status, message, progress, files, error)

### 5. إعدادات Next.js (next.config.ts):
```typescript
const nextConfig: NextConfig = {
  output: "standalone",
  experimental: {
    largePageDataBytes: 1024 * 1024 * 1024, // 1GB
  },
};
```

### 6. التصميم:
- خلفية داكنة (slate-900)
- ألوان emerald للأزرار والنجاح
- ألوان red للأخطاء
- ألوان amber للتقدم
- استخدام مكونات shadcn/ui (Button, Card, Progress, Badge, Alert)
- أيقونات lucide-react

### 7. المسارات المطلوبة:
- src/app/page.tsx (الواجهة الرئيسية)
- src/app/api/upload-system/route.ts (API الرفع والاستخراج)
- src/app/api/upload-system/status/route.ts (API حالة الاستخراج)
- src/app/api/upload-system/deploy/route.ts (API النشر والتشغيل)

### 8. الميزات الإضافية:
- تسجيل العمليات في console للتصحيح
- التعامل مع الأخطاء بشكل شامل
- زر إعادة المحاولة عند الخطأ
- عرض حجم الملف أثناء الرفع
- عرض عدد الملفات المستخرجة
```

---

## 📂 هيكل الملفات

```
src/
├── app/
│   ├── page.tsx                    # الواجهة الرئيسية
│   └── api/
│       └── upload-system/
│           ├── route.ts            # API الرفع والاستخراج
│           ├── status/
│           │   └── route.ts        # API حالة الاستخراج
│           └── deploy/
│               └── route.ts        # API النشر والتشغيل
```

---

## 🚀 طريقة الاستخدام

### 1. نسخ الملفات يدوياً:
```bash
# نسخ الواجهة
cp page.tsx src/app/

# إنشاء مجلدات API
mkdir -p src/app/api/upload-system/status
mkdir -p src/app/api/upload-system/deploy

# نسخ ملفات API
cp upload-route.ts src/app/api/upload-system/route.ts
cp status-route.ts src/app/api/upload-system/status/route.ts
cp deploy-route.ts src/app/api/upload-system/deploy/route.ts
```

### 2. تحديث next.config.ts:
أضف الإعدادات التالية لدعم الملفات الكبيرة:
```typescript
experimental: {
  largePageDataBytes: 1024 * 1024 * 1024, // 1GB
}
```

---

## 📦 الملفات المضمنة

| الملف | الوصف |
|-------|-------|
| `page.tsx` | واجهة الاستيراد الكاملة |
| `upload-route.ts` | API استخراج ملفات tar |
| `status-route.ts` | API مراقبة حالة الاستخراج |
| `deploy-route.ts` | API نشر وتشغيل النظام |

---

## ⚙️ المتطلبات الأساسية

- Next.js 14+ مع App Router
- TypeScript
- Tailwind CSS
- shadcn/ui components:
  - Button
  - Card
  - Progress
  - Badge
  - Alert

---

## 🔧 أوامر مفيدة

```bash
# تثبيت المكونات المطلوبة
bun add lucide-react

# تشغيل المشروع
bun run dev

# فحص الكود
bun run lint
```

---

## 📋 خطوات الاستخدام

1. **ارفع الملف** - اسحب أو اختر ملف .tar
2. **انتظر الاستخراج** - يتم فك ضغط الملفات تلقائياً
3. **شغل النظام** - انقر على زر "تشغيل النظام"
4. **استخدم النظام** - سيتم إعادة تحميل الصفحة

---

## 🆕 تحديثات v2.0

- دعم الملفات الكبيرة حتى 1GB
- تحسين معالجة الأخطاء
- إضافة سجلات للتصحيح
- زيادة timeout لأمر tar إلى 5 دقائق
- عرض حجم الملف أثناء الرفع
- تحسين واجهة المستخدم
